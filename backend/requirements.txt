# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Settings & validation
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Database
sqlalchemy>=2.0.25
alembic>=1.13.0
psycopg2-binary>=2.9.9

# Auth
python-jose[cryptography]>=3.3.0
httpx>=0.26.0
# Use the OS trust store for TLS — fixes Windows TLS interception (AV/VPN MITM)
# without disabling verification. No-op on Linux/Mac with proper CA setup.
truststore>=0.10.0

# Excel import
openpyxl>=3.1.2
